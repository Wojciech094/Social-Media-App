import type { Config } from 'tailwindcss';

const config: Config = {
  theme: {
    extend: {
      colors: {
        customBlue: '#545cf2',
      },
    },
  },
  // You can add other Tailwind config options here
};

export default config;
